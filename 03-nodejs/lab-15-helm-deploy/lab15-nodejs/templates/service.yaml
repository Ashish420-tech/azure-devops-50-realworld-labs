apiVersion: v1
kind: Service
metadata:
  name: {{ include "lab15-nodejs.fullname" . }}
  labels:
    {{- include "lab15-nodejs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lab15-nodejs.selectorLabels" . | nindent 4 }}
