apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "lab15-nodejs.fullname" . }}
  labels:
    {{- include "lab15-nodejs.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "lab15-nodejs.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "lab15-nodejs.name" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.containerPort }}
