# Lab-11: Docker Deployment (Basic)

This lab demonstrates manual deployment of a Dockerized Node.js application using `docker run`.

## Steps
- Build Docker image locally
- Run container with port mapping
- Verify application via browser/curl

This lab focuses on deployment fundamentals before CI/CD automation.
